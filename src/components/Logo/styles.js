import {
  StyleSheet
} from 'aphrodite'

const styles = StyleSheet.create({
  container: {
    width: '10%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
  }
});

export default styles;