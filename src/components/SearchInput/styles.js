import {
  StyleSheet
} from 'aphrodite';

const styles = StyleSheet.create({
  container: {
    width: '30%',
    height: '60%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
  },
  input: {
    height: '100%',
    border: 0,
  },
  select: {
    height: '100%',
    border: 0,
  },
  button: {
    height: '100%',
    border: 0,
  }
});

export default styles;