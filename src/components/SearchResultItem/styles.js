import {
  StyleSheet
} from 'aphrodite';
import { fonts, colors } from '../../settings';

const styles = StyleSheet.create({
  container: {
    width: 300,
    marginLeft: 20,
    marginBottom: 20
  },
});

export default styles;